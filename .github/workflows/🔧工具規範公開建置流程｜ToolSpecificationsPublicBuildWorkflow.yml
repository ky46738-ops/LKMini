name: 🔧工具規範公開建置

on:
  workflow_dispatch:

jobs:
  legal_hold:
    name: 🧑‍⚖️法院證據保全暫停狀態
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: 🧊保持暫停
        run: echo "LEGAL_HOLD_AUTOMATION_PAUSED"
