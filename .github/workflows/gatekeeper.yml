name: 🛡️公開種子守門驗證

on:
  workflow_dispatch:

jobs:
  legal_hold:
    name: 🧑‍⚖️法院證據保全暫停狀態
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: 🧊保持暫停
        run: echo "LEGAL_HOLD_AUTOMATION_PAUSED"
